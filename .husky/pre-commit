#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Lint staged
pnpm exec lint-staged || exit 1

# Secret scan staged content
node scripts/precommit-secret-scan.js || exit 1
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

pnpm exec lint-staged
